import padScore from 'ember-pong/utils/pad-score';
import { module, test } from 'qunit';

module('Unit | Utility | pad score');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = padScore();
  assert.ok(result);
});
